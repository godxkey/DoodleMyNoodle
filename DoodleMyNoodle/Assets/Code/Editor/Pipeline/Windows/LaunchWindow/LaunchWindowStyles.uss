:root {
    margin-left: 5px;
    margin-right: 5px;
    margin-top: 5px;
    overflow:visible;
}

.title {
    font-size: 20px;
    font-weight: bold;
}

.title {
    font-size: 20px;
    font-weight: bold;
}

.category{
    margin-bottom: 20px;
}

.buildButton{
    flex:1;
}

#refreshButton {
    position: absolute;
    right: 2px;
    top: 0px;
    width: 55px;
}

#buildButtonsContainer{
    flex-direction:row;
}
#buildButtonSpacing{
    width:4px;
}